# Server (production — adapter-node)
PORT=3000
HOST=0.0.0.0
ORIGIN=http://localhost:3000

# App defaults (optional, pre-fills form fields)
PUBLIC_DEFAULT_COMPANY_CODE=
PUBLIC_DEFAULT_EMPLOYEE_NAME=
PUBLIC_DEFAULT_EMPLOYEE_ID=
